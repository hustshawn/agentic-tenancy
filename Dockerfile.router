FROM golang:1.24-alpine AS builder
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -ldflags="-s -w" -o /bin/router ./cmd/router

FROM gcr.io/distroless/static-debian12
COPY --from=builder /bin/router /router
ENTRYPOINT ["/router"]
